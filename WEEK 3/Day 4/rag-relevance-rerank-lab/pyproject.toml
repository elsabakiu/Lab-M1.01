[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-relevance-rerank-lab"
version = "0.1.0"
description = "Week 3 Day 4 lab scaffold for relevance scoring and rerankers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
