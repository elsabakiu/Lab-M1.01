[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lab1-chunking"
version = "0.1.0"
description = "Week 2 Day 5 Lab 1 starter: chunking strategies for RAG"
readme = "README.md"
requires-python = ">=3.11"

dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
